# ========== 核心依赖 ==========
# PyTorch深度学习框架
torch>=1.8.0
torchvision>=0.9.0

# 数值计算
numpy>=1.19.0
scipy>=1.5.0

# 数据处理
pandas>=1.1.0

# 可视化
matplotlib>=3.3.0

# ========== 可选依赖 ==========
# TensorBoard (训练监控，可选但推荐)
tensorboard>=2.4.0

# 进度条 (可选)
tqdm>=4.50.0

# ========== 安装说明 ==========
# 1. 基本安装（所有依赖）:
#    pip install -r requirements.txt
#
# 2. 最小安装（不含TensorBoard）:
#    pip install torch torchvision numpy scipy pandas matplotlib
#
# 3. GPU版本的PyTorch（推荐）:
#    访问 https://pytorch.org/ 根据你的CUDA版本安装
#    例如: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
